<launch>
  <!-- Use Gazebo sim time (set early) -->
  <param name="use_sim_time" value="true"/>

  <env name="GAZEBO_MODEL_PATH"
       value="$(find pallet_camera_gz)/models:$(optenv GAZEBO_MODEL_PATH)" />

  <include file="$(find gazebo_ros)/launch/empty_world.launch">
    <arg name="world_name" value="$(find pallet_camera_gz)/gz_worlds/pallet_camera.world"/>
    <arg name="gui" value="true"/>
  </include>

  <!-- Publish world -> stand_link (and stand_link -> rgbd_camera_link if your script does that) -->
  <node pkg="pallet_camera_gz" type="publish_camera_tf.py"
        name="camera_stand_tf_publisher" output="screen"/>

  <!-- Publish rgbd_camera_link -> rgbd_camera_optical_frame (static REP-103 optical frame) -->
  <node pkg="tf2_ros" type="static_transform_publisher"
        name="rgbd_optical_tf"
        args="0 0 0 -1.57079632679 0 -1.57079632679 rgbd_camera_link rgbd_camera_optical_frame"/>
</launch>